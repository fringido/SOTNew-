import { CommandColorDirective } from './command-color.directive';

describe('CommandColorDirective', () => {
  it('should create an instance', () => {
    // const directive = new CommandColorDirective();
    // expect(directive).toBeTruthy();
  });
});
